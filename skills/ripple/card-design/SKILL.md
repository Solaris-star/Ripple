---
name: card-design
description: >
  社媒卡片视觉设计系统：提供配色、中文字体层级、满画幅布局、品类骨架和死空白/密度质检，避免模板化 PPT 与廉价 AI 感。
  当用户说“卡片难看、优化视觉、封面/海报设计、排版配色、卡片填不满、AI 味重”时使用；所有 card-*、poster-hero 等视觉产出应把它作为设计规范，而非最终渲染器。
layer: produce
---

# 卡片设计系统（创建/改变视觉方案时使用）

> 这是一套设计方法和检查基线。新建卡片、改变布局/配色/字体系统时读取；同一项目只改文字、数据或局部素材时复用已经锁定的设计 spec，不必重新通读或重新选风格。用户明确的品牌规范和参考图优先于预设风格。

## 五步流程（照做，别跳）

1. **确定视觉立场**：用户已指定品牌/风格就直接用；同一项目已有 spec 就复用。都没有时按内容推荐一个合理默认并说明，只有多个方向会显著改变交付时才让用户选择。`references/styles.md` 是灵感和基线，不要求每次先做一次风格问卷。
2. **保持一致**：字体、配色、网格和装饰在一套卡片中应有一致逻辑；预设 spec 可作为起点。用户品牌规范、明确参考图或可访问性要求与预设冲突时，以用户约束为准，不为“锁色”牺牲品牌一致性。
3. **定字体层级**：按 `typography.md`——「**字越大越细**」（字体已装 Noto Sans/Serif CJK 全字重，大标题用 Thin/Light）。手机端正文 ≥28px。
4. **套骨架**：从 `card-recipes.md` 选卡型骨架(封面/账本/管线/对比/矩阵/数据/金句)，按其**最小密度线**填内容。
5. **渲染后自检**：布局/渲染发生变化时运行 `card_audit.py`。文字裁切、元素越界、不可读、尺寸错误等客观问题必须修复；密度、留白比例、骨架偏好属于设计诊断，结合用户意图判断，不为通过阈值而凭空增加内容或改画幅。局部文字修改只复核受影响卡片；共享 CSS/模板变化再扩大检查范围。

## 铁律速查（详见 references）

- **留白有意图**：知识密集卡通常需要较高内容密度；大面积无意留白值得检查，但 75%/15% 只作诊断参考。不要为了过密度阈值编造信息、擅改用户指定画幅或添加无关装饰。→ `layout-laws.md`
- **字越大越细**:大标题 w200-500,小字才用粗体。全用 700 粗黑体 = 廉价 banner。→ `typography.md`
- **禁 emoji 当图标**:用线性图标(Lucide,stroke 1.5,棱角款)或纯排版。→ `anti-ai-slop.md`
- **禁蓝紫科技渐变**(头号 AI tell)、禁 `bg-clip-text` 渐变字、禁玻璃拟态、禁"居中一切"。→ `anti-ai-slop.md`
- **正文永远不用纯黑**,用深灰/深墨(#6B6560 / #0a1f3d 等)。
- 瑞士立场:**零圆角、零阴影、禁渐变**,靠色块+发丝线+网格。杂志立场:小圆角、仅截图给极柔阴影、必须有背景氛围层(极淡纸纹/墨晕)。

## references

- `styles.md` — 风格库；新建/改视觉方向或当前项目没有 spec 时按需读取，同一任务已经锁定风格后不重复加载。
- `palettes.md` — 锁定配色细节(杂志6套 + 瑞士4套 + 莫兰迪/奶油补充),带 hex。
- `typography.md` — 中文字体层级、"越大越细"、最小字号死线、中英混排(Noto Sans/Serif CJK 全字重)。
- `layout-laws.md` — 填满画幅法则、4带密度自检、欠填/溢出修正阶梯(治死空白)。
- `anti-ai-slop.md` — AI 廉价感 P0/P1/P2 反例清单 + 高杠杆去 AI 味动作。
- `card-recipes.md` — 按卡型/品类的骨架 + 每种的最小密度线。

## Profile 感知

- **有 Profile**:从 `style.md` 读品牌配色/风格倾向;但**优先保证本系统的高级感底线**——若 Profile 没给明确视觉规范,就按本系统选立场+锁色,别退回"柔和渐变"这类模糊描述。account 名用作水印。
- **无 Profile**:知识/科技类默认「瑞士 + 克莱因蓝」或「杂志 + Indigo Porcelain」;生活/情感类默认「杂志 + Kraft/Dune 暖纸」。

## 参考来源
