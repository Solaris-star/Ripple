import { tool } from "@opencode-ai/plugin"
const base=()=>process.env.RIPPLE_TOOL_BASE||"";const token=()=>process.env.RIPPLE_AGENT_TOOL_TOKEN||"";
export default tool({description:"使用 Ripple 中已配置的视频 Provider/模型生成视频并保存到素材与成品。可指定 Provider 和模型；留空使用用户设置的默认组合。不会发布。",args:{prompt:tool.schema.string().min(1).max(12000),ratio:tool.schema.string().max(10).optional(),duration:tool.schema.number().int().min(1).max(60).optional(),provider_id:tool.schema.string().max(64).optional(),model:tool.schema.string().max(200).optional()},async execute(args){const r=await fetch(base()+"/api/ripple-agent/tools/media/video",{method:"POST",headers:{"Content-Type":"application/json","X-Ripple-Agent-Token":token()},body:JSON.stringify({prompt:args.prompt,ratio:args.ratio||"9:16",duration:args.duration,provider_id:args.provider_id,model:args.model})});const t=await r.text();if(!r.ok)throw new Error(`Ripple tool failed (${r.status})`);return t}})
