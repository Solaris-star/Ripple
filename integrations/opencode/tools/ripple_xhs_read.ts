import { tool } from "@opencode-ai/plugin"
const base=()=>process.env.RIPPLE_TOOL_BASE||"";const token=()=>process.env.RIPPLE_AGENT_TOOL_TOKEN||"";
async function post(body:unknown){const r=await fetch(base()+"/api/ripple-agent/tools/xiaohongshu/read",{method:"POST",headers:{"Content-Type":"application/json","X-Ripple-Agent-Token":token()},body:JSON.stringify(body)});const t=await r.text();if(!r.ok)throw new Error(`Ripple tool failed (${r.status})`);return t}
export default tool({description:"读取 Ripple 已连接小红书账号的可用只读数据：账号列表、个性化推荐流样本、关键词搜索、自己的近期作品、指定笔记详情或评论。推荐流仅代表所选账号当前样本，不是全平台热榜。",args:{operation:tool.schema.string(),account_id:tool.schema.string().optional(),query:tool.schema.string().optional(),note_id:tool.schema.string().optional(),url:tool.schema.string().optional(),limit:tool.schema.number().int().min(1).max(100).optional()},async execute(args){return post({operation:args.operation,account_id:args.account_id||"",query:args.query||"",note_id:args.note_id||"",url:args.url||"",limit:args.limit||12})}})
