import { tool } from "@opencode-ai/plugin"
const base=()=>process.env.RIPPLE_TOOL_BASE||"";const token=()=>process.env.RIPPLE_AGENT_TOOL_TOKEN||"";
async function post(body:unknown){const r=await fetch(base()+"/api/ripple-agent/tools/interactions/draft",{method:"POST",headers:{"Content-Type":"application/json","X-Ripple-Agent-Token":token()},body:JSON.stringify(body)});const t=await r.text();if(!r.ok)throw new Error(`Ripple tool failed (${r.status})`);return t}
export default tool({description:"为 Ripple 已接入互动能力的真实平台账号创建待确认互动草稿。必须绑定受支持的平台/账号/作品或已同步评论；不接受本地导入评论或通用来源。此工具绝不执行真实发送或删除，用户必须到「互动管理」完整审阅。",args:{platform:tool.schema.string(),account_id:tool.schema.string().optional(),source_id:tool.schema.string().optional(),target_id:tool.schema.string().optional(),target_url:tool.schema.string().optional(),kind:tool.schema.string(),items:tool.schema.array(tool.schema.object({id:tool.schema.string().optional(),nickname:tool.schema.string().optional(),content:tool.schema.string().optional(),reply:tool.schema.string().optional()})).max(20).optional(),text:tool.schema.string().max(1000).optional()},async execute(args){return post({platform:args.platform,account_id:args.account_id||"",source_id:args.source_id||"",target_id:args.target_id||"",target_url:args.target_url||"",kind:args.kind,items:args.items||[],text:args.text||""})}})
